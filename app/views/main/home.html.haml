-if flash[:notice]
  =flash[:notice]
  
%h2 Current Medic Count
%table
  %thead
    %tr
      %th Time
      %th --
      %th City Medics
      %th -- 
      %th M131
      %th M241
  %tbody
    -MedicRecord.limit(10).order("id desc").each do |rec|
      %tr
        %td= rec.created_at.strftime("%I:%M %p")
        %td==--
        %td
          %strong==  #{rec.count}
      
%br
=button_to "Graphs","/graphs"
